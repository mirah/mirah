#!/usr/bin/env jruby
begin
  require 'duby'
rescue LoadError
  $: << File.dirname(File.dirname(__FILE__)) + '/lib'
  require 'duby'
end

puts 'WARNING: Duby is now Mirah. Please use the `mirah` command.'
Duby.run(*ARGV)
